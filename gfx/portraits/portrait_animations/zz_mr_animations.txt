###################################
# Default Personality Animations  #
###################################

TRY_INJECT:idle = {
	male = {

		### MODDED

		mr_chemist_test_tube_idle = {
			animation = { head = "idle_head_pop_hattop" torso = "idle_body_walkingcane" }

			weight = {
				base = 0


				modifier = {
					add = 50
					scope:character ?= {
						has_role = character_role_mendelejew_chemist
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = test_tube_attachment	 }
		}

		mr_chemist_erlenmeyer_idle = {
			animation = { head = "idle_head_pop_hattop" torso = "idle_body_walkingcane" }

			weight = {
				base = 0

				modifier = {
					add = 50
					year >= 1860
					scope:character ?= {
						has_role = character_role_mendelejew_chemist
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = erlen_attachment	 }
		}

		mr_composer_conducting_idle = {
			animation = { head = "idle_head_pop_saber" torso = "idle_body_composer_conducting" }

			weight = {
				base = 0

				modifier = {
					add = 50
					scope:character ?= {
						has_variable = mr_is_composer_var
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = conductor_stick_attachment	 }
		}

		mr_writer_ledger_idle = {
			animation = { head = "idle_head_emotions_happy" torso = "idle_body_ledger" }

			weight = {
				base = 0

				modifier = {
					add = 50
					scope:character ?= {
						has_variable = mr_is_writer_var
					}
				}
			}
			portrait_modifier = { animations_props = ledger_attachment }
		}

		mr_physician_ledger_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_ledger" }

			weight = {
				base = 0

				modifier = {
					add = 50
					scope:character ?= {
						has_role = character_role_panum_physician
					}
				}
			}
			portrait_modifier = { animations_props = ledger_attachment }
		}

		mr_painter_brush_palette_idle = {
			animation = { head = "idle_head_pop_saber" torso = "idle_body_painter" }

			weight = {
				base = 0

				modifier = {
					add = 50
					scope:character ?= {
						has_variable = mr_is_painter_var
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = painter_attachment }
		}


		mr_archeologist_mattock_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_archeo_01" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						OR = {
							has_variable = mr_is_archaeologist_var
							has_role = character_role_dubois_paleontologist
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = archeo_01_attachment }
		}

		mr_archeologist_trowel_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_archeo_02" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						OR = {
							has_variable = mr_is_archaeologist_var
							has_role = character_role_dubois_paleontologist
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = archeo_02_attachment }
		}


		mr_anthropologist_skull_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_anthro" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						has_role = character_role_andersson_anthropologist
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = anthro_attachment }
		}

		mr_geologist_rock_01_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_geolog" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						has_role = character_role_agassiz_geologist
						NOT = {
							owner ?= {
								has_journal_entry = je_agassiz_volcano_expedition
							}
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = geolog_01_attachment }
		}

		mr_geologist_rock_02_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_anthro" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						has_role = character_role_agassiz_geologist
						NOT = {
							owner ?= {
								has_journal_entry = je_agassiz_volcano_expedition
							}
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = geolog_02_attachment }
		}

		mr_mechanic_wrench_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_wrench" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						has_variable = mr_is_mechanic_var
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = mechanic_wrench_attachment }
		}

		gaudi_architect_plans_idle = {
			animation = { head = "idle_head_pop_saber" torso = "idle_body_architect_plans" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						OR = {
							has_variable = mr_is_architect_var
							has_variable = mr_is_independent_architect_var
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = mr_architect_plans }
		}

		gaudi_architect_set_square_idle = {
			animation = { head = "idle_head_pop_saber" torso = "idle_body_architect_thinking" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						OR = {
							has_variable = mr_is_architect_var
							has_variable = mr_is_independent_architect_var
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = mr_architect_set_square }
		}

		gaudi_architect_pencil_idle = {
			animation = { head = "idle_head_pop_saber" torso = "idle_body_architect_pencil" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						OR = {
							has_variable = mr_is_architect_var
							has_variable = mr_is_independent_architect_var
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = mr_architect_pencil }
		}
	}

	female = {
		### MODDED
		mr_chemist_test_tube_idle = {
			animation = { head = "idle_entry" torso = "idle_body_walkingcane" }

			weight = {
				base = 0

				modifier = {
					add = 50
					scope:character ?= {
						has_role = character_role_mendelejew_chemist
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = test_tube_attachment	 }
		}

		mr_chemist_erlenmeyer_idle = {
			animation = { head = "idle_entry" torso = "idle_body_walkingcane" }

			weight = {
				base = 0

				modifier = {
					add = 50
					year >= 1860
					scope:character ?= {
						has_role = character_role_mendelejew_chemist
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = erlen_attachment	 }
		}

		mr_composer_conducting_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_composer_conducting" }

			weight = {
				base = 0

				modifier = {
					add = 50
					scope:character ?= {
						has_variable = mr_is_composer_var
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = conductor_stick_attachment	 }
		}

		mr_writer_ledger_idle = {
			animation = { head = "idle_head_emotions_happy" torso = "idle_body_ledger" }

			weight = {
				base = 0

				modifier = {
					add = 50
					scope:character ?= {
						has_variable = mr_is_writer_var
					}
				}
			}
			portrait_modifier = { animations_props = ledger_attachment }
		}

		mr_physician_ledger_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_ledger" }

			weight = {
				base = 0

				modifier = {
					add = 50
					scope:character ?= {
						has_role = character_role_panum_physician
					}
				}
			}
			portrait_modifier = { animations_props = ledger_attachment }
		}

		mr_painter_brush_palette_idle = {
			animation = { head = "idle_head_pop_hatacademic" torso = "idle_body_painter" }

			weight = {
				base = 0

				modifier = {
					add = 50
					scope:character ?= {
						has_variable = mr_is_painter_var
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = painter_attachment }
		}


		mr_archeologist_mattock_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_archeo_01" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						OR = {
							has_variable = mr_is_archaeologist_var
							has_role = character_role_dubois_paleontologist
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = archeo_01_attachment }
		}

		mr_archeologist_trowel_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_archeo_02" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						OR = {
							has_variable = mr_is_archaeologist_var
							has_role = character_role_dubois_paleontologist
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = archeo_02_attachment }
		}


		mr_anthropologist_skull_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_anthro" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						has_role = character_role_andersson_anthropologist
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = anthro_attachment }
		}

		mr_geologist_rock_01_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_geolog" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						has_role = character_role_agassiz_geologist
						NOT = {
							owner ?= {
								has_journal_entry = je_agassiz_volcano_expedition
							}
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = geolog_01_attachment }
		}

		mr_geologist_rock_02_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_anthro" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						has_role = character_role_agassiz_geologist
						NOT = {
							owner ?= {
								has_journal_entry = je_agassiz_volcano_expedition
							}
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = geolog_02_attachment }
		}

		mr_mechanic_wrench_idle = {
			animation = { head = "idle_head_pop_wrench" torso = "idle_body_wrench" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						has_variable = mr_is_mechanic_var
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = mechanic_wrench_attachment }
		}

		gaudi_architect_plans_idle = {
			animation = { head = "idle_head_pop_shovels" torso = "idle_body_architect_plans" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						OR = {
							has_variable = mr_is_architect_var
							has_variable = mr_is_independent_architect_var
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = mr_architect_plans }
		}

		gaudi_architect_set_square_idle = {
			animation = { head = "idle_head_pop_shovels" torso = "idle_body_architect_thinking" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						OR = {
							has_variable = mr_is_architect_var
							has_variable = mr_is_independent_architect_var
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = mr_architect_set_square }
		}

		gaudi_architect_pencil_idle = {
			animation = { head = "idle_head_pop_shovels" torso = "idle_body_architect_pencil" }

			weight = {
				base = 0

			modifier = {
					add = 50
					scope:character ?= {
						OR = {
							has_variable = mr_is_architect_var
							has_variable = mr_is_independent_architect_var
						}
					}
				}
			}
			portrait_modifier = { mr_test_animations_props = mr_architect_pencil }
		}
    }
}
