### SPORTS BUTTONS ###

#by Lord R & Marco Dandolo

#Does the country have a car racer
mr_has_car_racer_sgui = {
	scope = country
	is_shown = {
		has_variable = vikelas_racer_var
	}
}

#MR_AUTORACING_LONG_DISTANCE_BUTTON
mr_autoracing_long_distance_button = {
    scope = country
	is_shown = {
		has_variable = vikelas_automobile_club_var
	}
    is_valid = {
		mr_gui_has_combustion_engine_tech_trigger = yes
		mr_gui_has_organized_sports_tech_trigger = yes
		custom_tooltip = {
			text = MR_AUTORACING_LONG_DISTANCE_NOT_ONGOING_TRIGGER_TT
			NOT = {
				has_global_variable = vikelas_endurance_race_ongoing_global_var
			}
		}
		custom_tooltip = {
			text = MR_AUTORACING_LONG_DISTANCE_MODIFIER_TRIGGER_TT
			NOT = {
				has_modifier = vikelas_endurance_race_hosted_modifier
			}
		}
		custom_tooltip = {
			text = MR_AUTORACING_LONG_DISTANCE_STATE_TRIGGER_TT
			any_scope_state = {
				count >= 5
			}
		}
    }
    effect = {
		set_global_variable = vikelas_endurance_race_ongoing_global_var
		add_modifier = {
			name = vikelas_endurance_race_hosting_modifier
		}
		trigger_event = {
			id = vikelas.531
		}
    }
	ai_is_valid = { always = yes }	# Whether this SGUI is available to the AI or not (default false)
	ai_chance = {
		base = 100 # The chance that the AI will activate this SGUI (script value between 1 and 100)
	}
	ai_frequency = 12 # Named value determining how frequently the AI will evaluate this SGUI (in months)
}

#MR_AUTORACING_NO_PARTICIPATION_BUTTON
mr_autoracing_no_participation_button = {
    scope = country
	is_shown = {
		has_variable = vikelas_automobile_club_var
		NOT = {
			has_variable = vikelas_racer_does_not_participate_var
		}
	}
    is_valid = {
		custom_tooltip = {
			text = MR_HAS_RACER_TT
			has_variable = vikelas_racer_var
		}
    }
    effect = {
		custom_tooltip = {
			text = vikelas_no_participation_tt
			set_variable = vikelas_racer_does_not_participate_var
		}
    }
}

#MR_AUTORACING_PARTICIPATION_BUTTON
mr_autoracing_participation_button = {
    scope = country
	is_shown = {
		has_variable = vikelas_racer_does_not_participate_var
	}
    is_valid = {
		custom_tooltip = {
			text = MR_HAS_RACER_TT
			has_variable = vikelas_racer_var
		}
    }
    effect = {
		custom_tooltip = {
			text = vikelas_participation_tt
			remove_variable = vikelas_racer_does_not_participate_var
		}
    }
}

#######################################################################################################################

#MR_AUTORACING_FOUND_AUTOMOBILE_CLUB_BUTTON
mr_autoracing_found_automobile_club_button = {
    scope = country
	is_shown = {
		NOT = {
			has_variable = vikelas_automobile_club_var
		}
	}
    is_valid = {
		mr_gui_has_combustion_engine_tech_trigger = yes
    }
    effect = {
		set_variable = vikelas_automobile_club_var
		hidden_effect = {
			if = {
				limit = { has_variable = vikelas_automobile_club_var }
				create_new_racer_effect = yes
				vikelas_set_new_racer_as_racer_effect = yes
			}
		}
		trigger_event = {
			id = vikelas.501
		}
    }
	ai_is_valid = { always = yes }	# Whether this SGUI is available to the AI or not (default false)
	ai_chance = {
		base = 100 # The chance that the AI will activate this SGUI (script value between 1 and 100)
	}
	ai_frequency = 6 # Named value determining how frequently the AI will evaluate this SGUI (in months)
}

#MR_AUTORACING_FOUND_AIACR_BUTTON
mr_autoracing_found_aiacr_button = {
	scope = country
	is_shown = {
		NOT = {
			has_global_variable = vikelas_autoracing_aiacr_founded_global_var
		}
		has_variable = vikelas_automobile_club_var
	}
	is_valid = {
		mr_gui_has_combustion_engine_tech_trigger = yes
		mr_gui_has_organized_sports_tech_trigger = yes
		custom_tooltip = {
			text = MR_AUTORACING_FOUND_AIACR_BUTTON_TRIGGER_TT
			any_country = {
				has_variable = vikelas_automobile_club_var
				count >= 10
			}
		}
	}
	effect = {
		custom_tooltip = vikelas_become_aiacr_president_tt
		set_global_variable = vikelas_autoracing_aiacr_founded_global_var
		set_variable = vikelas_aiacr_founding_country_var
		capital = {
			create_building = {
				building = building_vikelas_aiacr
				level = 1
			}
			state_region = {
				set_global_variable = { #This is used in the GUI to display the state
            		name = vikelas_aiacr_seat_global_var
           			value = this
        		}
			}
		}
		country_definition = {
			set_global_variable = {
				name = vikelas_aiacr_president_global_var
				value = ROOT
			}
		}
		hidden_effect = {
			every_country = {
				limit = {
					has_variable = vikelas_automobile_club_var
				}
				trigger_event = {
					id = vikelas.503
				}
			}
		}
	}
	ai_is_valid = { always = yes }	# Whether this SGUI is available to the AI or not (default false)
	ai_chance = {
		base = 100 # The chance that the AI will activate this SGUI (script value between 1 and 100)
	}
	ai_frequency = 6 # Named value determining how frequently the AI will evaluate this SGUI (in months)
}

#MR_AUTORACING_RACE_TRACK_BUTTON
mr_autoracing_race_track_button = {
	scope = country
	is_shown = {
		NOT = {
			any_scope_state = {
				any_scope_building = {
					is_building_type = building_vikelas_circuit
				}
			}
		}
		has_variable = vikelas_automobile_club_var
	}
	is_valid = {
		mr_gui_has_combustion_engine_tech_trigger = yes
		mr_gui_has_paved_roads_tech_trigger = yes
	}
	effect = {
		vikelas_add_circuit_effect = yes
	}
	ai_is_valid = { always = yes }	# Whether this SGUI is available to the AI or not (default false)
	ai_chance = {
		base = 100 # The chance that the AI will activate this SGUI (script value between 1 and 100)
	}
	ai_frequency = 6 # Named value determining how frequently the AI will evaluate this SGUI (in months)
}

#MR_AUTORACING_FOUND_GRAND_PRIX_BUTTON
mr_autoracing_found_grand_prix_button = {
	scope = country
	is_shown = {
		NOT = {
			has_global_variable = vikelas_autoracing_grand_prix_founded_var
		}
		has_global_variable = vikelas_autoracing_aiacr_founded_global_var
		has_variable = vikelas_automobile_club_var
	}
	is_valid = {
		mr_gui_has_organized_sports_tech_trigger = yes
		#mr_gui_has_mass_culture_tech_trigger = yes
		custom_tooltip = {
			text = MR_HAS_RACE_TRACK_TT
			any_scope_building = {
				is_building_type = building_vikelas_circuit
			}
		}
		custom_tooltip = {
			text = MR_AUTORACING_FOUND_GRAND_PRIX_BUTTON_TRIGGER_TT
			any_country = {
				has_variable = vikelas_automobile_club_var
				has_technology_researched = organized_sports
				count >= 5
			}
		}
	}
	effect = {
		set_global_variable = vikelas_autoracing_grand_prix_founded_var
		set_variable = vikelas_gp_founder_var
		if = {
			limit = {
				NOT = {
					has_variable = vikelas_aiacr_founding_country_var
				}
			}
			custom_tooltip = vikelas_aiacr_board_member_tt
		}
		random_scope_state = {
			limit = {
				is_production_method_active = {
					building_type = building_vikelas_circuit
					production_method = pm_vikelas_normal_circuit
				}
			}
			activate_production_method = {
				building_type = building_vikelas_circuit
				production_method = pm_vikelas_grand_prix_circuit
			}
			ROOT = {
				country_definition = {
					set_global_variable = {
						name = vikelas_grand_prix_country_1_global_var
						value = ROOT
					}
				}
			}
			state_region = {
				set_global_variable = { #This is used in the GUI to display the state
            		name = vikelas_grand_prix_circuit_1_global_var
           			value = this
        		}
				set_variable = vikelas_grand_prix_circuit_1_state_var
			}
		}
		custom_tooltip = {
			text = vikelas_add_1_grand_prix_track_tt
			set_global_variable = {
				name = vikelas_num_grand_prix_tracks_global_var
				value = 1
			}
		}
		hidden_effect = {
			every_country = {
				limit = {
					has_variable = vikelas_automobile_club_var
				}
				trigger_event = {
					id = vikelas.502
				}
			}
		}
	}
	ai_is_valid = { always = yes }	# Whether this SGUI is available to the AI or not (default false)
	ai_chance = {
		base = 100 # The chance that the AI will activate this SGUI (script value between 1 and 100)
	}
	ai_frequency = 6 # Named value determining how frequently the AI will evaluate this SGUI (in months)
}

#MR_AUTORACING_FOUND_TEAM_BUTTON
mr_autoracing_found_team_button = {
	scope = country
	is_shown = {
		NOT = {
			has_variable = vikelas_autoracing_team_var
		}
		has_variable = vikelas_automobile_club_var
	}
	is_valid = {
		custom_tooltip = {
			text = MR_HAS_RACER_TT
			has_variable = vikelas_racer_var
		}
		custom_tooltip = {
			text = MR_HAS_ENGINEER_TT
			gaudi_has_engineer = yes
		}
		custom_tooltip = {
			text = MR_AUTORACING_FOUND_TEAM_BUTTON_TRIGGER_TT
			any_scope_building = {
				is_building_type = building_motor_industry
				has_active_production_method = pm_automobile_production
				level >= 2
			}
		}
	}
	effect = {
		set_variable = vikelas_autoracing_team_var
		trigger_event = {
			id = vikelas.505
		}
	}
	ai_is_valid = { always = yes }	# Whether this SGUI is available to the AI or not (default false)
	ai_chance = {
		base = 100 # The chance that the AI will activate this SGUI (script value between 1 and 100)
	}
	ai_frequency = 6 # Named value determining how frequently the AI will evaluate this SGUI (in months)
}

#MR_AUTORACING_RACE_TRACK_BUTTON
mr_autoracing_race_track_button = {
	scope = country
	is_shown = {
		NOT = {
			any_scope_state = {
				any_scope_building = {
					is_building_type = building_vikelas_circuit
				}
			}
		}
		has_variable = vikelas_automobile_club_var
	}
	is_valid = {
		mr_gui_has_combustion_engine_tech_trigger = yes
		mr_gui_has_organized_sports_tech_trigger = yes
		mr_gui_has_paved_roads_tech_trigger = yes
		custom_tooltip = {
			text = MR_HAS_RACER_TT
			has_variable = vikelas_racer_var
		}
		custom_tooltip = {
			text = MR_HAS_ENGINEER_TT
			has_variable = gaudi_engineer_var
		}
	}
	effect = {
		vikelas_add_circuit_effect = yes
	}
	ai_is_valid = { always = yes }	# Whether this SGUI is available to the AI or not (default false)
	ai_chance = {
		base = 100 # The chance that the AI will activate this SGUI (script value between 1 and 100)
	}
	ai_frequency = 6 # Named value determining how frequently the AI will evaluate this SGUI (in months)
}

#MR_AUTORACING_RACE_TRACK_APPLY_BUTTON
mr_autoracing_race_track_apply_button = {
	scope = country
	is_shown = {
		any_scope_state = {
			is_production_method_active = {
				building_type = building_vikelas_circuit
				production_method = pm_vikelas_normal_circuit
			}
		}
		has_global_variable = vikelas_autoracing_grand_prix_founded_var
		global_var:vikelas_num_grand_prix_tracks_global_var < 5
		NOT = {
			has_variable = vikelas_aiacr_founding_country_var
		}
	}
	is_valid = {
		custom_tooltip = {
			text = MR_MAXIMUM_5_RACE_TRACKS_TT
			global_var:vikelas_num_grand_prix_tracks_global_var < 5
		}
		custom_tooltip = {
			text = MR_HAS_RACE_TRACK_TT
			any_scope_building = {
				is_building_type = building_vikelas_circuit
			}
		}
	}
	effect = {
		if = {
			limit = {
				NOT = {
					has_variable = vikelas_aiacr_founding_country_var
				}
			}
			custom_tooltip = vikelas_aiacr_board_member_tt
		}
		random_scope_state = {
			limit = {
				is_production_method_active = {
					building_type = building_vikelas_circuit
					production_method = pm_vikelas_normal_circuit
				}
			}
			activate_production_method = {
				building_type = building_vikelas_circuit
				production_method = pm_vikelas_grand_prix_circuit
			}
			if = {
				limit = {
					has_global_variable = vikelas_grand_prix_circuit_4_global_var
				}
				ROOT = {
					country_definition = {
						set_global_variable = {
							name = vikelas_grand_prix_country_5_global_var
							value = ROOT
						}
					}
				}
				state_region = {
					set_global_variable = { #This is used in the GUI to display the state
						name = vikelas_grand_prix_circuit_5_global_var
						value = this
					}
					set_variable = vikelas_grand_prix_circuit_5_state_var
				}
			}
			else_if = {
				limit = {
					has_global_variable = vikelas_grand_prix_circuit_3_global_var
				}
				ROOT = {
					country_definition = {
						set_global_variable = {
							name = vikelas_grand_prix_country_4_global_var
							value = ROOT
						}
					}
				}
				state_region = {
					set_global_variable = { #This is used in the GUI to display the state
						name = vikelas_grand_prix_circuit_4_global_var
						value = this
					}
					set_variable = vikelas_grand_prix_circuit_4_state_var
				}
			}
			else_if = {
				limit = {
					has_global_variable = vikelas_grand_prix_circuit_2_global_var
				}
				ROOT = {
					country_definition = {
						set_global_variable = {
							name = vikelas_grand_prix_country_3_global_var
							value = ROOT
						}
					}
				}
				state_region = {
					set_global_variable = { #This is used in the GUI to display the state
						name = vikelas_grand_prix_circuit_3_global_var
						value = this
					}
					set_variable = vikelas_grand_prix_circuit_3_state_var
				}
			}
			else_if = {
				limit = {
					has_global_variable = vikelas_grand_prix_circuit_1_global_var
				}
				ROOT = {
					country_definition = {
						set_global_variable = {
							name = vikelas_grand_prix_country_2_global_var
							value = ROOT
						}
					}
				}
				state_region = {
					set_global_variable = { #This is used in the GUI to display the state
						name = vikelas_grand_prix_circuit_2_global_var
						value = this
					}
					set_variable = vikelas_grand_prix_circuit_2_state_var
				}
			}
		}
		custom_tooltip = {
			text = vikelas_add_1_grand_prix_track_tt
			change_global_variable = {
				name = vikelas_num_grand_prix_tracks_global_var
				add = 1
			}
			random_country = {
				limit = {
					has_variable = vikelas_aiacr_founding_country_var
				}
				trigger_event = {
					id = vikelas.504
				}
			}
		}
	}
	ai_is_valid = { always = yes }	# Whether this SGUI is available to the AI or not (default false)
	ai_chance = {
		base = 100 # The chance that the AI will activate this SGUI (script value between 1 and 100)
	}
	ai_frequency = 6 # Named value determining how frequently the AI will evaluate this SGUI (in months)
}

#MR_AUTORACING_RACE_TRACK_DECLARE_BUTTON
mr_autoracing_race_track_declare_button = {
	scope = country
	is_shown = {
		any_scope_state = {
			is_production_method_active = {
				building_type = building_vikelas_circuit
				production_method = pm_vikelas_normal_circuit
			}
		}
		has_global_variable = vikelas_autoracing_grand_prix_founded_var
		has_variable = vikelas_aiacr_founding_country_var
	}
	is_valid = {
		custom_tooltip = {
			text = MR_IS_AIACR_PRESIDENT_TT
			has_variable = vikelas_aiacr_founding_country_var
		}
		custom_tooltip = {
			text = MR_HAS_RACE_TRACK_TT
			any_scope_building = {
				is_building_type = building_vikelas_circuit
			}
		}
	}
	effect = {
		random_scope_state = {
			limit = {
				is_production_method_active = {
					building_type = building_vikelas_circuit
					production_method = pm_vikelas_normal_circuit
				}
			}
			activate_production_method = {
				building_type = building_vikelas_circuit
				production_method = pm_vikelas_grand_prix_circuit
			}
			ROOT = {
				country_definition = {
					set_global_variable = {
						name = vikelas_grand_prix_country_aiacr_global_var
						value = ROOT
					}
				}	
			}
			state_region = {
				set_global_variable = { #This is used in the GUI to display the state
					name = vikelas_grand_prix_circuit_aiacr_global_var
					value = this
				}
				set_variable = vikelas_grand_prix_circuit_aiacr_state_var
			}
		}
		custom_tooltip = {
			text = vikelas_add_1_grand_prix_track_tt
			change_global_variable = {
				name = vikelas_num_grand_prix_tracks_global_var
				add = 1
			}
		}
	}
	ai_is_valid = { always = yes }	# Whether this SGUI is available to the AI or not (default false)
	ai_chance = {
		base = 100 # The chance that the AI will activate this SGUI (script value between 1 and 100)
	}
	ai_frequency = 6 # Named value determining how frequently the AI will evaluate this SGUI (in months)
}

#MR_AIACR_PRESIDENT_BUTTON
mr_aiacr_president_button = {
    scope = country
	is_shown = {
		has_global_variable = vikelas_autoracing_grand_prix_founded_var
		has_variable = vikelas_automobile_club_var
		NOT = {
			any_country = {
				has_variable = vikelas_aiacr_founding_country_var
			}
		}
	}
    is_valid = {
		custom_tooltip = {
			text = MR_AIACR_CONTROLS_HQ_TRIGGER_TT
			any_scope_state = {
				has_building = building_vikelas_aiacr
			}
		}
		custom_tooltip = {
			text = MR_IOC_FOUNDATION_PEACE_TRIGGER_TT
			is_at_war = no
		}
    }
    effect = {
		custom_tooltip = {
			text = vikelas_become_aiacr_president_tt
			set_variable = vikelas_aiacr_founding_country_var
		}
		country_definition = {
			set_global_variable = {
				name = vikelas_aiacr_president_global_var
				value = ROOT
			}
		}
    }

	ai_is_valid = { always = yes }	# Whether this SGUI is available to the AI or not (default false)
	ai_chance = {
		base = 100 # The chance that the AI will activate this SGUI (script value between 1 and 100)
	}
	ai_frequency = 6 # Named value determining how frequently the AI will evaluate this SGUI (in months)
}