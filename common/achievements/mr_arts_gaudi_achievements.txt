#######################
# GAUDÍ ACHIEVEMENTS  #
#######################

#by Marco Dandolo

###################

gaudi_kulturnation = {
	possible = {
		has_variable = gaudi_kulturnation_ach_possible
	}
	happened = {
		OR = {
			country_has_primary_culture = cu:south_german
			country_has_primary_culture = cu:north_german
		}
		has_variable = gaudi_dombau_finished
		has_variable = elgar_wagnerism_achievement
		has_variable = verrier_discovered_neptune_variable
		has_modifier = elgar_founded_musikverein_modifier
		has_modifier = elgar_expressionism_birthplace_modifier
		any_scope_character = {
			has_modifier = elgar_conductor_modifier
		}
		any_scope_character = {
			has_trait = lepsius_archaeologist_famous
		}
	}
}

gaudi_segundo_siglo_de_oro = {
	possible = {
		has_variable = gaudi_segundo_siglo_de_oro_ach_possible
	}
	happened = {
		exists = c:SPA
		this = c:SPA
		has_variable = gaudi_sagrada_finished_var
		has_variable = verrier_discovered_pluto_variable
		has_modifier = elgar_art_nouveau_birthplace_modifier
		var:lepsius_total_discoveries >= 1
		var:elgar_had_composers >= 1
	}
}

gaudi_rapture = {
	possible = {
	}
	happened = {
		has_modifier = gaudi_art_deco_splendour_modifier
		any_scope_building = {
			is_building_type = building_gaudi_people_monument
		}
		OR = {
			any_scope_building = {
				is_building_type = building_cristo_redentor
			}
			any_scope_building = {
				is_building_type = building_gaudi_palacio
			}
		}
	}
}

gaudi_stalin = {
	possible = {
	}
	happened = {
		has_building = building_gaudi_people_palace
		has_law = law_type:law_command_economy
        has_law = law_type:law_council_republic
	}
}