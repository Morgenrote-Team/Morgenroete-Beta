###########################
# THEILER PEST CONDITIONS #
###########################

theiler_boll_weevil_condition = {
	trigger = {
		theiler_state_can_have_boll_weevil_trigger = yes
	}

	incompatible_with = frost
	incompatible_with = wildfire
	incompatible_with = extreme_winds
	incompatible_with = hailstorm
	incompatible_with = torrential_rains
	# Incompatible with extreme weather

	range = {
		integer_range = {
			min = 3
			max = 5
		}
	}

	duration = {
		fixed_range = {
			min = 250
			max = 500
		}
	}

	modifier = {
		#Basic Boll Weevil Effects
		goods_output_fabric_mult = -0.25
	}

	intensity = {
		fixed_range = {
			min = 1
			max = 1
		}
	}

	chance = {
		value = 0.0025
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_cotton_plantation
					}
				}
				level >= 5
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_cotton_plantation
					}
				}
				level >= 10
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_cotton_plantation
					}
				}
				level >= 15
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_cotton_plantation
					}
				}
				level >= 20
			}
			multiply = 5
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_cotton_plantation
						has_active_production_method = automatic_irrigation_building_cotton_plantation
					}
				}
			}
			multiply = 3
		}
	}

	color = { 168 255 241 }
	icon = "gfx/interface/icons/harvest_condition_icons/theiler_condition_boll_weevil.dds"
	incompatible_terrain = { }
	map_texture = "gfx/map/map_painting/harvest_conditions_map_patterns/harvest_conditions_bad.dds"
	graphics = locust_swarm
}

theiler_codling_moth_condition = {
	trigger = {
		theiler_state_can_have_codling_moth_trigger = yes
	}

	incompatible_with = frost
	incompatible_with = wildfire
	incompatible_with = extreme_winds
	incompatible_with = hailstorm
	incompatible_with = torrential_rains
	# Incompatible with extreme weather

	range = {
		integer_range = {
			min = 3
			max = 3
		}
	}

	duration = {
		fixed_range = {
			min = 250
			max = 500
		}
	}

	modifier = {
		#Basic Codling Moth Effects
		goods_output_fruit_mult = -0.25
	}

	intensity = {
		fixed_range = {
			min = 1
			max = 1
		}
	}

	chance = {
		value = 0.0025
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						OR = {
							is_building_type = building_wheat_farm
							is_building_type = building_maize_farm
							is_building_type = building_rye_farm
						}
						OR = {
							has_active_production_method = pm_apple_orchards
							has_active_production_method = pm_citrus_orchards #normally the parasite would not attack these, but wheat farms should be affected, too.
						}
						level >= 5
					}
				}
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						OR = {
							is_building_type = building_wheat_farm
							is_building_type = building_maize_farm
							is_building_type = building_rye_farm
						}
						OR = {
							has_active_production_method = pm_apple_orchards
							has_active_production_method = pm_citrus_orchards #normally the parasite would not attack these, but wheat farms should be affected, too.
						}
						level >= 10
					}
				}
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						OR = {
							is_building_type = building_wheat_farm
							is_building_type = building_maize_farm
							is_building_type = building_rye_farm
						}
						OR = {
							has_active_production_method = pm_apple_orchards
							has_active_production_method = pm_citrus_orchards #normally the parasite would not attack these, but wheat farms should be affected, too.
						}
						level >= 15
					}
				}
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						OR = {
							is_building_type = building_wheat_farm
							is_building_type = building_maize_farm
							is_building_type = building_rye_farm
						}
						OR = {
							has_active_production_method = pm_apple_orchards
							has_active_production_method = pm_citrus_orchards #normally the parasite would not attack these, but wheat farms should be affected, too.
						}
						level >= 20
					}
				}
			}
			multiply = 5
		}
	}

	color = { 122 255 89 }
	icon = "gfx/interface/icons/harvest_condition_icons/theiler_condition_codling_moth.dds"
	incompatible_terrain = { }
	map_texture = "gfx/map/map_painting/harvest_conditions_map_patterns/harvest_conditions_bad.dds"
	graphics = locust_swarm
}

theiler_grape_phylloxera_condition = {
	trigger = {
		theiler_state_can_have_grape_phylloxera_trigger = yes
	}

	incompatible_with = frost
	incompatible_with = wildfire
	incompatible_with = extreme_winds
	incompatible_with = hailstorm
	incompatible_with = torrential_rains
	# Incompatible with extreme weather

	range = {
		integer_range = {
			min = 4
			max = 4
		}
	}

	duration = {
		fixed_range = {
			min = 250
			max = 500
		}
	}

	modifier = {
		#Basic Grape Phylloxera Effects
		goods_output_wine_mult = -0.25
	}

	intensity = {
		fixed_range = {
			min = 1
			max = 1
		}
	}

	chance = {
		value = 0.0025
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_vineyard
					}
				}
				level >= 5
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_vineyard
					}
				}
				level >= 10
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_vineyard
					}
				}
				level >= 15
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_vineyard
					}
				}
				level >= 20
			}
			multiply = 5
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_vineyard
						has_active_production_method = automatic_irrigation_building_vineyard
					}
				}
			}
			multiply = 3
		}
	}

	color = { 192 150 255 }
	icon = "gfx/interface/icons/harvest_condition_icons/theiler_condition_grape_phylloxera.dds"
	incompatible_terrain = { }
	map_texture = "gfx/map/map_painting/harvest_conditions_map_patterns/harvest_conditions_bad.dds"
	graphics = locust_swarm
}

theiler_potato_beetle_condition = {
	trigger = {
		theiler_state_can_have_potato_beetle_trigger = yes
	}

	#Mr. Potato Beetle doesn't know any incompabilities. He is the incompability.

	range = {
		integer_range = {
			min = 4
			max = 6
		}
	}

	duration = {
		fixed_range = {
			min = 250
			max = 500
		}
	}

	modifier = {
		#Basic Potato Beetle Effects
		goods_output_grain_mult = -0.25
	}

	intensity = {
		fixed_range = {
			min = 1
			max = 1
		}
	}

	chance = {
		value = 0.0025
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						OR = {
							is_building_type = building_wheat_farm
							is_building_type = building_maize_farm
							is_building_type = building_rye_farm
						}
						level >= 5
					}
				}
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						OR = {
							is_building_type = building_wheat_farm
							is_building_type = building_maize_farm
							is_building_type = building_rye_farm
						}
						level >= 10
					}
				}
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						OR = {
							is_building_type = building_wheat_farm
							is_building_type = building_maize_farm
							is_building_type = building_rye_farm
						}
						level >= 15
					}
				}
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						OR = {
							is_building_type = building_wheat_farm
							is_building_type = building_maize_farm
							is_building_type = building_rye_farm
						}
						level >= 20
					}
				}
			}
			multiply = 5
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_rye_farm
						has_active_production_method = pm_potatoes
					}
				}
			}
			multiply = 10 #Nom Nom
		}
	}

	color = { 255 131 30 }
	icon = "gfx/interface/icons/harvest_condition_icons/theiler_condition_potato_beetle.dds"
	incompatible_terrain = { }
	map_texture = "gfx/map/map_painting/harvest_conditions_map_patterns/harvest_conditions_bad.dds"
	graphics = locust_swarm
}

theiler_screwworm_fly_condition = {
	trigger = {
		theiler_state_can_have_screwworm_fly_trigger = yes
	}

	#Mr. The Screwworm Fly doesn't know any incompabilities. She is the incompability.

	range = {
		integer_range = {
			min = 5
			max = 7
		}
	}

	duration = {
		fixed_range = {
			min = 250
			max = 500
		}
	}

	modifier = {
		#Basic Scre Worm Fly Effects
		goods_output_meat_mult = -0.25
	}

	intensity = {
		fixed_range = {
			min = 1
			max = 1
		}
	}

	chance = {
		value = 0.0025
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_livestock_ranch
					}
				}
				level >= 5
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_livestock_ranch
					}
				}
				level >= 10
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_livestock_ranch
					}
				}
				level >= 15
			}
			multiply = 2
		}
		if = {
			limit = {
				any_scope_state = {
					any_scope_building = {
						is_building_type = building_livestock_ranch
					}
				}
				level >= 20
			}
			multiply = 5
		}
	}

	color = { 255 71 89 }
	icon = "gfx/interface/icons/harvest_condition_icons/theiler_condition_screwworm_fly.dds"
	incompatible_terrain = { }
	map_texture = "gfx/map/map_painting/harvest_conditions_map_patterns/harvest_conditions_bad.dds"
	graphics = locust_swarm
}